function findConnectors (node){
	/*Finds all nodes connected to a given node during its lifetime. i.e. All requirements fulfilled by a given source or all sources used by a given requirement*/
}

function calcInterest (node) {
	/*calculates the total interest return/cost for a node over its lifetime*/
}

function findBestMatch (node) {
	/*Finds the best possible match fr a given (requirement) node regardless of the rest of the solution. Useful if user wants to know the effect of sacrificing overall solution fitness to favor a specific requirement*/
}

